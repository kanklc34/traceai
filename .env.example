# Get your API key from https://aistudio.google.com/
GEMMA_API_KEY=your_api_key_here

# Only Gemma models are used — no Gemini dependency

# Set to 1 only for local dev without API key (shows evidence-only RCA)
TRACEAI_DEV_MOCK=0
